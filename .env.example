# Optional — live schedule providers are dormant unless SCHEDULE_LIVE=1 (not used in default UI).
# See docs/research/SCHEDULE-DATA.md appendix.
# Copy to apps/web/.env.local — never commit real keys.
SCHEDULE_LIVE=
AVIATIONSTACK_ACCESS_KEY=
AERODATABOX_RAPIDAPI_KEY=
AERODATABOX_RAPIDAPI_HOST=aerodatabox.p.rapidapi.com

# Route research spikes only (Jun 2026 — Kiwi /data/routes returned 404; optional Search API follow-up)
# KIWI_TEQUILA_API_KEY=
