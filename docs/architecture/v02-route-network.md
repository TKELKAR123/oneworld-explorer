# v0.2 route network (bootstrap)

```bash
npm run build:routes   # → data/eligible-routes.index.json
```

`packages/schedules/src/openflights-routes.ts` loads the index for `routeExists()` checks.

Full schedule adapters remain v0.2 — see [schedule-rpc-sequence.md](./schedule-rpc-sequence.md).
