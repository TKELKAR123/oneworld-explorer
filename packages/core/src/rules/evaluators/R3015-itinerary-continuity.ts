import type { EvaluationContext } from "../../ontology/types.js";

/** Continuity enforced at parse; passing evaluation implies chain is intact. */
export function evaluateR3015_itinerary_continuity(_ctx: EvaluationContext) {
  return [];
}
